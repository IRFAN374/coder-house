import React from 'react'

const Otp = ({nextHandler}) => {
    return (
        <>
            <div>
                I am OTP
            </div>
            <button onClick={nextHandler}>next</button>
        </>
    )
}

export default Otp
